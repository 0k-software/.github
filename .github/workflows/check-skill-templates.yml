name: Check skill templates in sync

on:
  pull_request:
    paths:
      - ".github/ISSUE_TEMPLATE/[0-9]*.yml"
      - ".claude/skills/0k-create-issue/templates/[0-9]*.yml"
  push:
    branches: [main]
    paths:
      - ".github/ISSUE_TEMPLATE/[0-9]*.yml"
      - ".claude/skills/0k-create-issue/templates/[0-9]*.yml"

jobs:
  check-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Check templates are in sync
        run: bin/check-skill-templates
